const darkButton = document.querySelector('[data-js="dark-mode"]');

darkButton.addEventListener('click', () => {
    
    const root = document.documentElement;

    //things will be happening here...

})